name: "pruebaaction"
description: "Una acción de GitHub que saluda a un usuario"
inputs:
  name:
    description: "El nombre del usuario a saludar"
    required: true
    default: "Mundo"
  nombreentorno:
    description: "El nombre del entorno de ejecución"
    required: true

runs:
  using: 'node20'
  main: 'dist/index.js'